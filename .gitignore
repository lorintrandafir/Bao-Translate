# OS
.DS_Store

# Android / Gradle build output
build/
.gradle/
.gradle-local/
.cxx/
.externalNativeBuild/
captures/
local.properties
*.iml
.idea/

# Large vendored native dependency — re-obtain to build (per maintainer decision)
Android/src/app/libs/sherpa-onnx-v1.13.2.aar

# Build artifacts
*.apk
*.aab
*.ap_
*.dex
*.class

# Secrets / signing material
*.jks
*.keystore
keystore.properties
*.p12
*.pem
*.env
.env
.env.*

# Scratch / runtime
.tmp/
.claude/
.cursor/
.kotlin/
*.log
*.hprof

# Agent-loop state and scratch doc bundles pulled in beside the app; not project sources
nes-bao-first-state.json
llms-stack-refresh/

# One-off device/emulator capture screenshots — attach to an issue, do not commit
bluestacks_*.png
screenshot_*.png

# Skills are synced from skills/ via Gradle preBuild; do not commit the copy
Android/src/app/src/main/assets/skills/
